/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/api/client";
import type { HealthReadyHealthReadyGetQueryResponse } from "../types/HealthReadyHealthReadyGet.ts";
import type { Client, RequestConfig, ResponseErrorConfig } from "@/api/client";

function getHealthReadyHealthReadyGetUrl() {
  const res = { method: 'GET', url: `/health/ready` as const }
  return res
}

/**
 * @description Readiness probe: can this instance actually serve traffic right now?
 * Pings Postgres and Redis directly (unlike `/health/live`, which is a
 * static response) so an orchestrator can route around an instance whose
 * dependencies are unreachable, e.g. mid-database-restart. Depends on the
 * same `get_db`/`get_redis` dependencies used elsewhere so tests can swap
 * in fakes the same way they do for every other route.
 * @summary Health Ready
 * {@link /health/ready}
 */
export async function healthReadyHealthReadyGet(config: Partial<RequestConfig> & { client?: Client } = {}) {
  const { client: request = fetch, ...requestConfig } = config



  const res = await request<HealthReadyHealthReadyGetQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getHealthReadyHealthReadyGetUrl().url.toString(), ... requestConfig })
  return res.data
}